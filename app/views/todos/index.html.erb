<div class="flex justify-between">
    <h1 class="text-2xl font-bold tracking-wide">Todos</h1>
    <%= link_to "New Todo", new_todo_path, data: {turbo_frame: "first_frame"},class: "px-4 py-2 bg-lime-500 rounded-lg" %>
</div>

<hr class="border-2 border-black mb-4"/>

<%= turbo_frame_tag "new_todo"%>

<%= render @todos %>